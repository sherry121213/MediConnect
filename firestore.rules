rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fully permissive rules for development to resolve all permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
