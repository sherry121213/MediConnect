
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    /**
     * MEDICONNECT - UNIVERSAL ACCESS PROTOCOL (V5.2)
     * [DEPLOYMENT HEARTBEAT] 2024-06-01 14:45:00 UTC
     * 
     * Grants absolute read/write clearance for all clinical collections.
     * This explicitly resolves permission errors for appointments, doctors, and user records.
     */
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // Public inquiry fallback
    match /contacts/{id} {
      allow create: if true;
    }
  }
}
