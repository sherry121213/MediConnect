rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * PRECISION_SESSION_ACCESS_V7
     * Global read/write access for prototype development.
     * Ensures clinical workflows are not interrupted by security handshakes.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}